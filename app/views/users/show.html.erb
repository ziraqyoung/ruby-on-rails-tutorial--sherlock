<% provide(title: @user.name ) %>
<div class="row">
  <aside class="user-info bg-secondary text-white mt-2 mr-2 p-3 rounded-lg col-sm-4">
    <div class="row no-gutters">
      <div class="col-sm-6">
        <div class="card">
          <%= gravatar_for( @user, class: 'rounded card-img-top' ) %>
        </div>
      </div>

      <div class="col-sm-6 text-center">
        <h2><%= @user.name %></h2>
      </div>
    </div>
  </aside>

    <div class="col-sm-7 bg-secondary text-white mt-2 p-3">
      this is for users microposts 
    </div>
  </div>

